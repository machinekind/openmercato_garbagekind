import type { ModuleSetupConfig } from '@open-mercato/shared/modules/setup'

/** Bez uprawnień — patrz `acl.ts`. */
export const setup: ModuleSetupConfig = { defaultRoleFeatures: {} }

export default setup
