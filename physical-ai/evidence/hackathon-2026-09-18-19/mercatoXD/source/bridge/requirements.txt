# Core: claiming tasks and driving the panel.
aiohttp>=3.9

# --policy g05 only (already present in the DGX inference venv).
websockets>=12.0
msgpack>=1.0
numpy>=1.26
opencv-python>=4.9

# Tests.
pytest>=8.0
pytest-asyncio>=0.23
pytest-cov>=5.0
